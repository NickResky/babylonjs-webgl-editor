import { injectable } from 'inversify';

/**
 * ControlsController
 */
@injectable()
export class ControlsController {
  /**
   * Constructor
   */
  constructor() {
    // throw new Error('Unimplemented');
  }
}
