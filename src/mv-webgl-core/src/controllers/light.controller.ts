import { injectable } from 'inversify';

/**
 * LightController
 */
@injectable()
export class LightController {
  /**
   * Constructor
   */
  constructor() {
    // throw new Error('Unimplemented');
  }
}
